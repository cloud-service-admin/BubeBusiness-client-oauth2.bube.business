// import { useAppSelector } from '@store/hooks';
// import { appSelect } from '@/features/app/app.slice';
// import { Spin } from 'antd';

// export default function SpinFullScreen_component() {
//   const spinFullScreenState = useAppSelector(appSelect).spinFullScreen;

//   return spinFullScreenState && <Spin fullscreen={spinFullScreenState} />;
// }
